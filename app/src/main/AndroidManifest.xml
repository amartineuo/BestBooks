<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bestbooks">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BestBooks">
        <activity android:name=".UserFavsActivity"></activity>
        <activity android:name=".UserBooksActivity" />
        <activity android:name=".ModifyBookActivity" />
        <activity android:name=".DetailBookActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ModifyProfileActivity" />
        <activity android:name=".AddBookActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".PrincipalActivity" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>